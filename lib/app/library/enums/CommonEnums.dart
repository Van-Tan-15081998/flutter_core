enum ActionModeEnum { create, update, delete, copy }

enum TaskStatusEnum {
  newStatus,
  pendingStatus,
  inProgressStatus,
  completedStatus,
  failedStatus,
  cancelledStatus,
  pausedStatus,
  scheduledStatus,
  overdueStatus
}
