import 'package:flutter/material.dart';

class CoreLargeBadge extends StatefulWidget {
  const CoreLargeBadge({
    super.key
  });

  @override
  State<CoreLargeBadge> createState() => _CoreLargeBadgeState();
}

class _CoreLargeBadgeState extends State<CoreLargeBadge> {

  @override
  Widget build(BuildContext context) {
    return const Text('_Core');
  }
}